const Header = () => {
  return (
    <header className="sticky top-0 flex w-full drop-shadow-sm">
      <div className=" flex flex-grow items-center justify-between py-4 px-4 shadow-sm">
        Header
      </div>
    </header>
  );
};

export default Header;
